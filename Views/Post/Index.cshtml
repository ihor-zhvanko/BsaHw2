@model IList<Post>
@{
    ViewData["Title"] = "All Posts";
}

<h2 class="my-5 text-center">All Posts</h2>

<div class="row">

  @foreach(var post in Model) {
    @await Html.PartialAsync("_PostCard", (@post, "Details", "Post", "Details"))
  }

</div>